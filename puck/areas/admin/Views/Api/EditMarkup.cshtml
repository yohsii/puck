@model puck.core.Base.BaseModel
@*model puck.areas.admin.ViewModels.Home*@

@{Layout=null;}

@using (Html.BeginForm("NewContent", "Api", new {p_type=Model.GetType().AssemblyQualifiedName }, FormMethod.Post))
{
    @Html.EditorForModel()
    <input type="submit" />
}