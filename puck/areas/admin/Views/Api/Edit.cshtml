@model puck.core.Base.BaseModel

@{
    Layout=null;
    ViewBag.Level0Type = Model.GetType();
}
<div class="content_edit_page">
@using (Html.BeginForm("Edit", "Api", new {p_type=Model.GetType().AssemblyQualifiedName,p_path=Request["p_path"] }, FormMethod.Post))
{
    @Html.EditorForModel()
    
    <button class="content_update btn btn-link">update</button>
}
</div>